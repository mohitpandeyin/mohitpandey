export type HowSustainzoneWorksProps = {
  title?: string
  buttonText?: string
  buttonLink?: string
  step1?: {
    heading: string
    keyPoints: Array<{ point: string }>
  }
  step2?: {
    heading: string
    keyPoints: Array<{ point: string }>
  }
  step3?: {
    heading: string
    keyPoints: Array<{ point: string }>
  }
}
