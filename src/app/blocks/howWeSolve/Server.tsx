import React from 'react'
import { HowWeSolve } from './HowWeSolve'

const HowWeSolveBlockServer: React.FC<any> = ({ heading, features }) => {
  return <HowWeSolve heading={heading} features={features} />
}

export default HowWeSolveBlockServer
